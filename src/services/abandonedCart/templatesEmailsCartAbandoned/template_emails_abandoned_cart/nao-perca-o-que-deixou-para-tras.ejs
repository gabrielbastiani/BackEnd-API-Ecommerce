<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>emails</title>
    <style type="text/css">
        h1 {
            font-size: 20px;
        }

        img {
            width: 50px;
            height: 50px;
        }
    </style>
</head>

<body>
    <h1>Olá, <%= name %>!</h1>
    <p>Vai deixar mesmo de comprar esses produtos?.</p>
    <p>Veja os produtos que perdeu abaixo</p>
    <div style="display: flex; flex-direction: column;">
        <% for(var i = 0; i < list_product.length; i++) {%>
            <img src="http://localhost:3333/files/<%= list_product[i].product.photoproducts[0].image %>" alt="<%= list_product[i].product.name %>">
            <span>
                <%= list_product[i].product.name %>
            </span>
        <% } %>
    </div>
</body>

</html>